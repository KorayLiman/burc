import 'package:flutter/material.dart';

class BurcDetail extends StatelessWidget {
  const BurcDetail({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}